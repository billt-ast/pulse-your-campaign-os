# configs/

Shared configuration fragments (eslint presets, tsconfig bases, prettier
overrides) that would live at the workspace root in a real monorepo. Kept
here so extraction is a move, not a rewrite.
