/**
 * design service — Design token, theme and accessibility contracts.
 *
 * Import contracts from `@/services/design` to build typed server fns and
 * client forms. Implementations live in future `*.functions.ts` files under
 * this folder. See ../README.md for boundary rules.
 */
export * from "./contracts";
