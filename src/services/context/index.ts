/**
 * context service — Runtime context resolution: org, workspace, mission, geography, locale.
 *
 * Import contracts from `@/services/context` to build typed server fns and
 * client forms. Implementations live in future `*.functions.ts` files under
 * this folder. See ../README.md for boundary rules.
 */
export * from "./contracts";
