# libs/

Low-level platform libraries. Database access, event bus, queues, logging,
observability, security, permissions, HTTP, errors, cache, environment,
constants. `libs/*` may import `packages/*` but never `services/*` or
`routes/*`.
